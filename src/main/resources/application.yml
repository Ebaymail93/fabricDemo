logging:
  level:
    root: 'INFO'
  pattern:
    console: '%d{dd-MM-yyyy HH:mm:ss} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n'
external-api:
  url:
    baseUrl: 'https://sandbox.platfr.io/api/gbs/banking/v4.0/accounts/'
    balance: '/balance'
    transaction: '/transactions'
    moneyTransfer: '/payments/money-transfers'
  apiKey: 'FXOVVXXHVCPVPBZXIJOBGUGSKHDNFRRQJP'
  accountId: '14537780'
  keyId: '3202'
  authSchema: 'S2S'
spring:
  output:
    ansi:
      enabled: always
  datasource:
    url: 'jdbc:h2:file:${user.home}/Desktop/fabricDB/fabricDB;DATABASE_TO_UPPER=false'
    username: 'fabric'
    password: 'demo'
    driverClassName: 'org.h2.Driver'
  jpa:
    spring.jpa.database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddlAuto: create
  h2:
    console.enabled: true
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources.add-mappings: false